# Garage Management System - Environment Configuration
# Copy this file to .env and configure for your environment

# Server Configuration
PORT=5000

# Security
SECRET_KEY=your-secret-key-here-change-in-production
PRODUCTION=false

# CORS Settings (comma-separated list of allowed origins)
# In development: * allows all origins
# In production: specify exact domains like https://yourdomain.com
ALLOWED_ORIGINS=*

# Production Deployment Notes:
# 1. Set PRODUCTION=true when deploying to production
# 2. Generate a secure SECRET_KEY (use: python3 -c "import secrets; print(secrets.token_hex(32))")
# 3. Configure ALLOWED_ORIGINS with your actual domain(s)
# 4. Use a reverse proxy (nginx, Apache) with HTTPS/SSL
# 5. Consider using a production database (PostgreSQL) instead of SQLite for high traffic
