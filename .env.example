# Garage Management System - Environment Variables
# Copy this file to .env and customize for your deployment

# Server Configuration
# PORT: The port the server will listen on
# Render, Heroku, and similar platforms set this automatically
PORT=5000

# HOST: The host address to bind to
# Use 0.0.0.0 for production to accept external connections
# Use 127.0.0.1 for local development only
HOST=0.0.0.0

# Database Configuration
# DB_FILE: Path to the SQLite database file
# Make sure this path is writable and persistent in production
DB_FILE=garage_management.db

# Python Version (for deployment platforms)
PYTHON_VERSION=3.11.0

# Note: Add .env to .gitignore to avoid committing sensitive data
